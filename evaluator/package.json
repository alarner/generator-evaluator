{
  "name": "evaluator",
  "version": "1.0.0",
  "description": "Accepts expressions from the generator, compute the result and then report the solution back to the generator.",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "test": "mocha --compilers js:babel-register",
    "build": "./node_modules/.bin/babel src -d dist"
  },
  "author": "Aaron Larner",
  "license": "MIT",
  "homepage": "https://github.com/alarner/bookshelf-api#readme",
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-es2015": "^6.5.0",
    "chai": "^3.5.0",
    "mocha": "^2.4.5"
  },
  "dependencies": {
    "connect": "^3.4.1",
    "morgan": "^1.7.0"
  }
}
