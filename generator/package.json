{
  "name": "generator",
  "version": "1.0.0",
  "description": "Send a series of random arithmetic expressions to the evaluator via get requests and reports the result.",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "test": "mocha --compilers js:babel-register",
    "build": "./node_modules/.bin/babel src -d dist"
  },
  "author": "Aaron Larner",
  "license": "MIT",
  "homepage": "https://github.com/alarner/bookshelf-api#readme",
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-es2015": "^6.5.0",
    "chai": "^3.5.0",
    "mocha": "^2.4.5"
  },
  "dependencies": {
    "request": "^2.69.0"
  }
}
